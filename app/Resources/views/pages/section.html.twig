{% extends ":default:index.html.twig" %}

{% block page %}

<!-- BODY -->
<section id="Body">
    <div class="row clearfix body-text">
        <h3 class="body-title">{{ section[0].title }}</h3>
        <div class="body-main-text">
            {% for employee in employees %}
                {{ include(':pages:employee.html.twig', { 'employee': employee }) }}<br/>
            {% endfor %}
        </div>
    </div>
</section>
<!-- /BODY -->

{% endblock %}